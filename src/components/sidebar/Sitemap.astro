---
import sitemap from '../../../public/sitemap.json'
import CollapsibleTree from './CollapsibleTree.astro'
---

{
  Object.entries(sitemap).map(([title, value]) => (
    <CollapsibleTree title={title} {value} />
  ))
}
